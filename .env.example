# Optionale Konfiguration. Die App läuft auch ganz ohne diese Variablen
# (dann greift die faktenbasierte Direktausgabe des KI-Assistenten).

# Lokales Ollama-LLM (z.B. im Amtsnetz). Wird nur genutzt, wenn erreichbar.
OLLAMA_URL="http://10.132.67.90:11434"
OLLAMA_MODEL="qwen3:30b"

# Optionaler Gemini-Fallback (nur aktiv, wenn ein Schlüssel gesetzt ist).
# GEMINI_API_KEY="..."
